FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx amadeus certifi

COPY amadeus-mcp/ ./amadeus-mcp/

ENV PYTHONPATH=/app

COPY amadeus-mcp/server.py ./server.py

CMD ["python", "-m", "uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8101"]
